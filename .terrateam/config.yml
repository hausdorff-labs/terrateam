engine:
  name: tofu
hooks:
  all:
    post:
    - cmd:
      - echo
      - FOO
      name: BAR
      type: env
    - capture_output: true
      cmd:
      - ${TERRATEAM_ROOT}/expect-env
      - BAR
      type: run
